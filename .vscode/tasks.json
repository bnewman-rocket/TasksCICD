{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Git Commit",
            "type": "shell",
            "command": "call \"${workspaceFolder}\\git_action.bat\" \"${input:commitMessage}\"",
            "options": {
                "cwd": "${workspaceFolder}",
                "shell": {
                    "executable": "cmd.exe",
                    "args": [
                        "/d",
                        "/c"
                    ]
                }
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "commitMessage",
            "type": "promptString",
            "description": "Commit message"
        }
    ]
}